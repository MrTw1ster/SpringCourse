server:
  port: 8189
  servlet:
    context-path: /app
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_lesson9?serverTimezone=Europe/Moscow&useSSL=false&allowMultiQueries=true
    username: root
    password: 4444
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
jwt:
  secret: qwerty